{
  "name": "exif-xmp-editor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Browser-based editor for GPS EXIF and Google Photo Sphere (GPano) XMP metadata on equirectangular JPEGs",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "verify": "esbuild scripts/verify-metadata.ts --bundle --platform=node --format=esm --outfile=scripts/verify-metadata.bundled.mjs && node scripts/verify-metadata.bundled.mjs && rm -f scripts/verify-metadata.bundled.mjs"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.22",
    "typescript": "~5.6.3",
    "vite": "^5.4.11"
  },
  "dependencies": {
    "@photo-sphere-viewer/core": "^5.15.1",
    "exifr": "^7.1.3",
    "leaflet": "^1.9.4"
  }
}
